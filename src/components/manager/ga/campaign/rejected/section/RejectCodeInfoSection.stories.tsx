/**
 * RejectCodeInfoSection 컴포넌트 스토리북
 */

import type { Meta, StoryObj } from "@storybook/react";
import RejectCodeInfoSection from "./RejectCodeInfoSection";

const meta: Meta<typeof RejectCodeInfoSection> = {
  title: "Manager/GA/Campaign/Rejected/Section/RejectCodeInfoSection",
  component: RejectCodeInfoSection,
  tags: ["autodocs"],
};

export default meta;

type Story = StoryObj<typeof RejectCodeInfoSection>;

export const Default: Story = {};

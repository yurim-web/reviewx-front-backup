/**
 * WithdrawalRequestFilterSection 컴포넌트 스토리북
 */

import type { Meta, StoryObj } from "@storybook/react";
import WithdrawalRequestFilterSection from "./WithdrawalRequestFilterSection";

const meta: Meta<typeof WithdrawalRequestFilterSection> = {
  title:
    "Manager/SA/Settlement/WithdrawalRequest/Section/WithdrawalRequestFilterSection",
  component: WithdrawalRequestFilterSection,
  tags: ["autodocs"],
};

export default meta;

type Story = StoryObj<typeof WithdrawalRequestFilterSection>;

export const Default: Story = {};



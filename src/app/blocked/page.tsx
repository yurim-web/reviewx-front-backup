/* ========================================
   🚫 차단된 회원 공용 페이지
   ======================================== */

/**
 * 차단된 회원 공용 페이지
 *
 * 페이지 경로:
 * - /blocked
 *
 * 사용 파일:
 * - 컴포넌트: BlockedUserPage
 */

import BlockedUserPage from "@/components/common/BlockedUserPage";

export default function BlockedPage() {
  return <BlockedUserPage />;
}

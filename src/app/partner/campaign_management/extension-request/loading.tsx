/* ========================================
   기한 연장 탭 로딩 컴포넌트
   ======================================== */

/**
 * ExtensionRequestLoading
 *
 * 목적: 기한 연장 탭 페이지 로딩 시 표시되는 로딩 컴포넌트
 *
 * 사용 페이지:
 * - /partner/campaign_management/extension-request
 */

import Loading from "@/app/loading";

export default function ExtensionRequestLoading() {
  return <Loading />;
}

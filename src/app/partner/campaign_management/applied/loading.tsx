/* ========================================
   신청내역 탭 로딩 컴포넌트
   ======================================== */

/**
 * AppliedLoading
 *
 * 목적: 신청내역 탭 페이지 로딩 시 표시되는 로딩 컴포넌트
 *
 * 사용 페이지:
 * - /partner/campaign_management/applied
 */

import Loading from "@/app/loading";

export default function AppliedLoading() {
  return <Loading />;
}

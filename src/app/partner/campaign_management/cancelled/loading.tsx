/* ========================================
   취소 탭 로딩 컴포넌트
   ======================================== */

/**
 * CancelledLoading
 *
 * 목적: 취소 탭 페이지 로딩 시 표시되는 로딩 컴포넌트
 *
 * 사용 페이지:
 * - /partner/campaign_management/cancelled
 */

import Loading from "@/app/loading";

export default function CancelledLoading() {
  return <Loading />;
}

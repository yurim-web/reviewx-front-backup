/* ========================================
   진행 탭 로딩 컴포넌트
   ======================================== */

/**
 * ProgressLoading
 *
 * 목적: 진행 탭 페이지 로딩 시 표시되는 로딩 컴포넌트
 *
 * 사용 페이지:
 * - /partner/campaign_management/progress
 */

import Loading from "@/app/loading";

export default function ProgressLoading() {
  return <Loading />;
}

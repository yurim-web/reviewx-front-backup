1순위: 포인트 거래 시스템 완성 (1-2일)

  - 문제: PointTransaction 엔티티만 있고 실제 저장 로직 누락
  - 작업:
    - PointTransactionService 완전 구현
    - 모든 포인트 거래 내역 DB 저장
    - 거래 전후 잔액 검증 로직
  - 영향: 파트너 포인트 차감, 리뷰어 보상 지급 불가능

  2순위: 기본 인증/권한 시스템 (1일)

  - 문제: X-User-Id 헤더만으로 인증 (보안 취약)
  - 작업:
    - 간단한 JWT 토큰 기반 인증
    - @PreAuthorize로 메서드 레벨 권한 검증
    - 로그인/회원가입 API
  - 영향: 실제 사용자 구분 불가능, 보안 위험

  3순위: 파일 업로드 시스템 (1일)

  - 문제: AttachedFile 엔티티만 있고 실제 업로드 처리 없음
  - 작업:
    - MultipartFile 처리 컨트롤러
    - 로컬 파일 저장 (프로토타입용)
    - 이미지 검증 (확장자, 크기)
  - 영향: 리뷰 증빙자료 업로드 불가능

  ⚡ 고우선 (High) - 프로토타입 품질 향상

  4순위: User 엔티티 리팩토링 (1-2일)

  - 문제: God Object 패턴 (60+ 필드), 도메인 경계 모호
  - 작업:
    - Partner, Reviewer 프로파일 분리
    - 비즈니스 로직을 Service로 이동
    - 도메인 서비스 패턴 적용
  - 영향: 유지보수성, 확장성 저하

  5순위: 데이터 검증 강화 (반나절)

  - 문제: Bean Validation 부재, 입력값 검증 미흡
  - 작업:
    - DTO에 @NotBlank, @Valid 등 어노테이션 추가
    - 커스텀 검증 로직 (날짜, 포인트 범위 등)
    - 에러 응답 표준화
  - 영향: 잘못된 데이터로 인한 오류